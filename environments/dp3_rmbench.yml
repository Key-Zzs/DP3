name: dp3-rmbench
channels:
  - nvidia/label/cuda-12.8.0
  - nvidia
  - defaults
dependencies:
  - python=3.10
  - pip
  - cuda-nvcc=12.8.61
  - cuda-cudart-dev=12.8.57
  # The repository bootstrap script is authoritative for the two-index
  # PyTorch install, CuRobo CUDA build, and editable DP3 install.
